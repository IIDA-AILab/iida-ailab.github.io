---
layout: default
---
<div class="post members-page">
  <article>
    {% if content and content != '' %}
      <div class="members-intro">{{ content }}</div>
    {% endif %}

    {% if page.member_sections %}
      {% for section in page.member_sections %}
        {% assign section_slug = section.title | slugify %}
        <section class="members-section" id="{{ section_slug }}">
          <div class="members-section-header">
            <h2>{{ section.title }}</h2>
          </div>

          <div
            class="members-grid{% if section.compact %} members-grid-compact{% endif %}"
            style="display:grid; grid-template-columns:repeat(auto-fit, minmax(220px, 1fr)); gap:1.25rem;"
          >
            {% for member in section.members %}
              <article class="member-card{% if member.featured %} member-card-featured{% endif %}{% if section.compact %} member-card-compact{% endif %}{% unless section.compact %} member-card-detailed{% endunless %}">
                <div class="member-card-media">
                  {% if member.image %}
                    {% assign member_image_path = member.image | prepend: 'assets/img/' %}
                    {% if member.image_circular == false %}
                      {% assign member_image_class = 'img-fluid z-depth-1 rounded' %}
                    {% else %}
                      {% assign member_image_class = 'img-fluid z-depth-1 rounded-circle' %}
                    {% endif %}
                    {% include figure.liquid loading="eager" path=member_image_path class=member_image_class sizes="(min-width: 768px) 280px, 90vw" alt=member.name %}
                  {% else %}
                    <div class="member-card-placeholder">{{ member.name | slice: 0 }}</div>
                  {% endif %}
                </div>

                <div class="member-card-body">
                  <div class="member-card-heading">
                    <h3>{{ member.name }}</h3>
                    {% if member.role %}
                      <p class="member-role">{{ member.role }}</p>
                    {% endif %}
                  </div>

                  {% if section.compact %}
                    {% if member.education and section.title != 'Alumni' %}
                      <div class="member-text-list">
                        {% for item in member.education %}
                          <p>{{ item }}</p>
                        {% endfor %}
                      </div>
                    {% endif %}

                    {% if member.research_areas %}
                      <div class="member-tag-list">
                        {% for item in member.research_areas %}
                          <span>{{ item }}</span>
                        {% endfor %}
                      </div>
                    {% endif %}

                    {% if member.phone or member.contact %}
                      <div class="member-text-list">
                        {% if member.phone %}
                          <p>{{ member.phone }}</p>
                        {% endif %}
                        {% if member.contact %}
                          {% for item in member.contact %}
                            <p>{{ item }}</p>
                          {% endfor %}
                        {% endif %}
                      </div>
                    {% endif %}

                    {% if section.title == 'Alumni' and member.current_affiliation %}
                      <div class="member-text-list">
                        <p>{{ member.current_affiliation }}</p>
                      </div>
                    {% endif %}
                  {% else %}
                    {% if member.education %}
                      <div class="member-detail-block">
                        <h4>Education</h4>
                        <ul>
                          {% for item in member.education %}
                            <li>{{ item }}</li>
                          {% endfor %}
                        </ul>
                      </div>
                    {% endif %}

                    {% if member.research_areas %}
                      <div class="member-detail-block">
                        <h4>Research Areas</h4>
                        <div class="member-tag-list">
                          {% for item in member.research_areas %}
                            <span>{{ item }}</span>
                          {% endfor %}
                        </div>
                      </div>
                    {% endif %}

                    {% if member.email or member.phone or member.office or member.website or member.github or member.contact %}
                      <div class="member-detail-block">
                        <h4>Contacts</h4>
                        <ul>
                          {% if member.email %}
                            <li>{{ member.email }}</li>
                          {% endif %}
                          {% if member.phone %}
                            <li>{{ member.phone }}</li>
                          {% endif %}
                          {% if member.office %}
                            <li>{{ member.office }}</li>
                          {% endif %}
                          {% if member.website %}
                            <li>{{ member.website }}</li>
                          {% endif %}
                          {% if member.github %}
                            <li>{{ member.github }}</li>
                          {% endif %}
                          {% if member.contact %}
                            {% for item in member.contact %}
                              <li>{{ item }}</li>
                            {% endfor %}
                          {% endif %}
                        </ul>
                      </div>
                    {% endif %}

                    {% if member.bio %}
                      <div class="member-bio">{{ member.bio | markdownify }}</div>
                    {% endif %}
                  {% endif %}

                  {% if member.email or member.website or member.github %}
                    <div class="member-links">
                      {% if member.email %}
                        <a href="mailto:{{ member.email }}" aria-label="{{ member.name }} email" title="Email">
                          <i class="fa-solid fa-envelope"></i>
                        </a>
                      {% endif %}
                      {% if member.website %}
                        <a href="{{ member.website }}" aria-label="{{ member.name }} website" title="Website">
                          <i class="fa-solid fa-house"></i>
                        </a>
                      {% endif %}
                      {% if member.github %}
                        <a href="{{ member.github }}" aria-label="{{ member.name }} GitHub" title="GitHub">
                          <i class="fa-brands fa-github"></i>
                        </a>
                      {% endif %}
                    </div>
                  {% endif %}
                </div>
              </article>
            {% endfor %}
          </div>
        </section>
      {% endfor %}
    {% endif %}
  </article>
</div>
